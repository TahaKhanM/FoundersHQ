# Plans

`/execute-v2` writes one plan per workstream here as `phase-{N}-{X}.md` before any code is written. Plans must be committed before implementation. See `superpowers:writing-plans` for structure.

The point: every implementation traces back to a written, reviewed plan. If you find code in this repo that doesn't, that's a bug.
